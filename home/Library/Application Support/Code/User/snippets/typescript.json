{
  "makets": {
    "prefix": "dax",
    "body": [
      "#!/usr/bin/env -S deno run --allow-all",
      "import { $ } from \"jsr:@david/dax\";",
      "",
      "async function main() {",
      "}",
      "",
      "if (import.meta.main) {",
      "    console.log(\"go: \");",
      "    $.setPrintCommand(true);",
      "    const start = performance.now();",
      "    await main();",
      "    const end = performance.now();",
      "    console.log(`ok! ${(end - start).toFixed(0)} ms`);",
      "}",
    ],
    "description": "dax"
  },
  "todo": {
    "prefix": "todo",
    "body": "throw new Error(\"todo\");"
  }
}

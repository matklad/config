#!/usr/bin/env not-bash

p.silent> 'git add .'
p> 'git diff --cached --color=always'
p> 'git show -s --oneline --abbrev-commit --color=always HEAD'

if confirm("Continue?"):
    p> 'git commit --amend --no-edit'

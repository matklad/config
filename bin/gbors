#!/home/matklad/config/not-bash.py

if len(argv) > 1:
    message = argv[1]
else:
    message = p('git show -s --format=%B HEAD', echo=False)

pr = p(f'git-hub pull new -m "{message}"')
pr = re.search(r'\[(\d+)\]', pr)[1]

p(f'git-hub pull comment -m "bors r+\n🤖" {pr}')
